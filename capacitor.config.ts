import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'tarea02_ionic_umsa',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
